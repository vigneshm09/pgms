# Frontend Setup

## Run locally

1. Install packages with `npm install`.
2. Copy `.env.example` to `.env`.
3. Set `VITE_API_URL=http://localhost:8000`.
4. Start the app with `npm run dev`.

The frontend runs on `http://localhost:5173`.
